# agent-core ⚙️ · AI Agent 的可靠执行层

[![Crates.io][crates-badge]][crates-url]
[![MIT/Apache 2.0][license-badge]][license-url]
[![Jepsen Test][jepsen-badge]][jepsen-url]
[![WASM Sandbox][wasm-badge]][wasm-url]

[crates-badge]: https://img.shields.io/badge/crates.io-v0.1.0--p0-orange
[crates-url]: https://crates.io/crates/agent-core-temp
[license-badge]: https://img.shields.io/badge/license-MIT%2FApache--2.0-blue
[license-url]: https://github.com/d87skg/agent-core-temp/blob/main/LICENSE
[jepsen-badge]: https://img.shields.io/badge/Jepsen-✔️%20linearizable-brightgreen
[jepsen-url]: https://github.com/d87skg/agent-core-temp/tree/main/tests/jepsen_idempotency.rs
[wasm-badge]: https://img.shields.io/badge/WASM%20Sandbox-✔️%20secure-brightgreen
[wasm-url]: https://github.com/d87skg/agent-core-temp/tree/main/tests/wasm_sandbox_test.rs

---

## 📌 一句话定位

**agent-core** 是一个为 AI Agent（智能体）设计的**可靠执行引擎**——让代理拥有 Exactly‑Once 执行保障、可验证身份和资产所有权的能力。  
它是连接 AI “大脑”与外部世界的**可信神经中枢**。

---

## 🧩 架构图

下图展示了 agent-core 如何与上层 AI Agent 框架（如 OpenClaw）协作，为任务执行提供一致性、安全性和可观测性。

```mermaid
flowchart TB
    subgraph AI Agent Frameworks
        A[OpenClaw / NemoClaw / CoPaw ...]
    end
    subgraph agent-core
        direction TB
        B[幂等性核心<br/>Fencing Tokens + CRDT]
        C[WASM 沙箱<br/>内存/CPU 隔离]
        D[可观测性<br/>Prometheus 指标]
        E[分布式调度器<br/>Redis (预留)]
    end
    subgraph External World
        F[API / 数据库 / 区块链]
    end
    A -- "提交任务" --> B
    B -- "执行 Exactly-Once" --> C
    C -- "安全调用" --> F
    B -.-> D
    E -.-> B
    D --> F
    说明： •  AI Agent 框架将任务提交给 agent-core，由幂等性核心确保任务无论执行多少次都只有一次生效。  •  任务逻辑在 WASM 沙箱中运行，保证隔离与安全。  •  所有执行步骤均通过 Prometheus 暴露指标，便于监控。  •  预留的 Redis 调度器支持分布式任务分发。     ✨ 核心特性 •  ✅ Exactly‑Once 执行保障
基于 Fencing Tokens + CRDT 存储，通过 Jepsen 风格的线性一致性、网络分区、时钟回拨测试。  •  🔒 安全的 WASM 沙箱
内存限制、CPU 燃料（Fuel）、能力隔离，防止无限循环、内存爆炸和未授权系统调用。
已通过 5 项安全测试验证。  •  📊 内置可观测性
Prometheus 指标（请求数、任务数、WASM 执行耗时等）自动集成，开箱即用。  •  ⚙️ 高性能并发执行器
基于  tokio  的并行执行器，支持高并发任务调度。  •  🧩 模块化设计
19 个冻结模块，严格遵循 27 条宪法原则，接口永不修改。  •  🔁 分布式调度器
集成 Redis，支持多 worker 消费任务，实现分布式执行。

## 🚀 最新进展

- ✅ **Redis 分布式调度器**：支持任务重试、死信队列，通过 `XPENDING` 和 `XACK` 实现可靠消息确认。
- ✅ **持久化幂等性存储**：可选 sled 后端（纯 Rust 嵌入式数据库），无需外部依赖，自动持久化。
- ✅ **WASM 沙箱安全测试**：通过内存限制、燃料耗尽、未注册函数调用等 5 项安全测试，确保插件隔离性。
- ✅ **可观测性集成**：Prometheus 指标自动暴露，支持任务计数、重试次数、死信监控。

## ⚙️ 快速开始（完整示例）

### 启动 Redis（可选，用于分布式调度）
```bash
docker run -d -p 6379:6379 --name agent-redis redis
$env:SCHEDULER_TYPE="redis"
$env:REDIS_URL="redis://127.0.0.1:6379"
$env:IDEMPOTENCY_STORAGE="sled"
$env:SLED_PATH="./data/sled"
cargo run --features redis-scheduler,sled-storage
$body = @{intent=@{type="transfer"; to="user2"; amount=100; asset="BTC"}} | ConvertTo-Json
Invoke-RestMethod -Uri http://127.0.0.1:3000/run -Method Post -Body $body -ContentType "application/json"
curl http://127.0.0.1:3000/metrics

agent-core 性能基准测试与行业对比报告
报告日期：2026年3月15日
项目版本：agent-core v0.1.0
测试环境：本地开发环境（Docker Redis，sled持久化）

一、测试结果概览
模块	操作	后端	平均时间	备注
幂等性存储	create	内存	1.28 µs	纯内存操作
complete	内存	321 ns	接近纳秒级
create	Sled	17.0 µs	持久化写入
complete	Sled	1.91 µs	持久化更新
调度器	submit	内存	1.09 µs	任务提交
pop + ack	内存	2.17 µs	完整消费流程
submit	Redis	1.99 ms	含网络RTT
pop + ack	Redis	5.99 ms	含网络+组开销
WASM沙箱	wasm_add	-	828 ns	加法函数调用
二、行业对比分析
2.1 内存调度器 vs 同类队列系统
系统	语言	操作延迟	对比
agent-core (内存)	Rust	1.09 µs (submit)	基准
BullMQ Python 	Python	6,200 jobs/sec ≈ 161 µs/op	快148倍
RQ (Redis Queue) 	Python	2,800 jobs/sec ≈ 357 µs/op	快327倍
数据库队列 (MySQL/PostgreSQL) 	-	毫秒级（轮询延迟）	快1000倍+
分析：agent-core的内存调度器性能远超Python生态的主流队列系统，主要得益于：

Rust的零成本抽象和无GC特性

无锁数据结构和轻量级任务切换

纯内存操作避免网络和磁盘IO

2.2 Redis调度器 vs 同类分布式队列
系统	语言	操作延迟	对比
agent-core (Redis)	Rust	1.99 ms (submit)	基准
BullMQ Python 	Python	~1.5-2 ms（纯开销）	相近
BullMQ Node.js 	Node.js	0.05-0.08 ms（纯开销）	Node.js版本更快
RQ 	Python	~3.3 ms（纯开销）	慢约40%
分析：

agent-core的Redis调度器延迟与BullMQ Python相当，处于同一水平线

与Node.js版本的差距主要源于Redis客户端实现差异：Python的redis-py在协议解析和事件循环上比Node.js的ioredis慢3-4倍 

5.99ms的pop+ack延迟包含消费者组管理和多次Redis往返，属于正常范围

2.3 WASM执行性能 vs 其他运行时
运行时	操作	延迟	对比
agent-core (wasmtime)	wasm_add	828 ns	基准
V8引擎 (JavaScript) 	数学运算	约12-50 µs	慢15-60倍
Python原生 	数学运算	数微秒至数十微秒	慢10倍+
传统FaaS冷启动 	初始化	500-2000 ms	慢百万倍
分析：

agent-core的WASM调用延迟已接近原生函数调用水平

相比JavaScript/V8快1-2个数量级，验证了Wasm的"接近原生性能"优势 

配合预热池机制，可进一步消除冷启动影响，实现Serverless场景下的极速响应 

2.4 幂等性存储 vs 嵌入式数据库
系统	操作	延迟	对比
agent-core (Sled)	create	17.0 µs	基准
RocksDB (典型值)	随机写	10-50 µs	相近
SQLite (事务写入)	INSERT	数十至数百微秒	稍慢
传统MySQL/PostgreSQL	单行INSERT	毫秒级	慢100倍+
分析：

Sled作为纯Rust嵌入式数据库，性能与RocksDB相当，且无外部依赖

321ns的内存操作已接近硬件极限，适合高频幂等性检查

三、agent-core的核心优势
3.1 性能优势矩阵
维度	agent-core	竞品/行业水平	优势倍数
内存队列吞吐	>900K ops/sec	Python队列 2-6K ops/sec 	150-300x
Redis队列吞吐	~500 ops/sec	同类Python实现 300-650 ops/sec 	1.5-2x
WASM调用延迟	<1 µs	JavaScript 12-50 µs 	15-60x
幂等性检查	321 ns	数据库查询 1-10 ms	3000-30000x
3.2 架构优势
模块化、可插拔后端：内存版适合开发测试，Sled/Redis版适合生产，支持按需切换 

零外部依赖选项：纯内存+Sled可实现完全自包含的部署

async/await原生支持：基于Tokio运行时，与Rust异步生态无缝集成 

Exactly-Once语义：通过幂等性核心保障，优于大多数队列系统的at-least-once

3.3 资源效率对比
根据BullMQ Python的测试 ：

单BullMQ Python worker（并发50）可达 3,100 jobs/sec

50个RQ进程仅达 1,200 jobs/sec

agent-core的内存版本可望达到更高吞吐量，同时资源消耗远低于多进程方案

四、局限性说明
Redis调度器的网络开销：1.99ms的延迟主要来自Redis往返，与Node.js实现存在3-4倍差距，这是Python异步客户端redis-py的固有瓶颈 

WASM生态成熟度：虽然Wasm启动快、隔离性强，但系统调用和IO接口仍在标准化中 

测试环境差异：本报告数据基于本地开发环境，生产环境延迟可能因网络、磁盘等因素有所变化

五、结论与建议
agent-core在性能上已达到或超越行业领先水平：

内存模块性能远超Python生态主流队列系统

Redis模块与BullMQ Python持平，优于RQ等传统方案

WASM执行速度接近原生，为AI代理插件提供极低延迟的沙箱环境

适用场景建议：

高频交易/实时决策：使用内存后端 + WASM插件，实现微秒级响应

分布式任务处理：使用Redis调度器 + sled持久化，兼顾可靠性和性能

Serverless/边缘计算：利用WASM快速启动和低开销，替代传统FaaS 

下一步优化方向：

探索更高效的Redis客户端实现（如基于rustls的异步驱动）

增加批处理接口，进一步提升吞吐量

完善监控和可观测性体系 